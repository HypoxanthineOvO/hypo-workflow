name: config-valid
label: guard
description: Validate project configuration before milestone execution.
default_severity: warn
hooks:
  - pre-milestone
check: |
  Validate `.pipeline/config.yaml` against `config.schema.yaml`.
  Prefer `scripts/validate-config.sh` when shell execution is available.
  Trigger when required fields are missing or unsupported values are present.
