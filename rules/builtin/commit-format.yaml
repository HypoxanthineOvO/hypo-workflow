name: commit-format
label: workflow
description: Check commit messages before creating commits.
default_severity: off
hooks:
  - pre-commit
check: |
  Validate commit messages against the configured workflow convention.
  Default recommendation is Conventional Commits, with Patch fixes using `fix(PNNN): <title>`.
  Trigger before committing when the message does not match the selected convention.
