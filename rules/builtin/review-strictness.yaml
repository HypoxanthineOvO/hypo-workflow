name: review-strictness
label: workflow
description: Apply the configured review strictness during evaluation.
default_severity: warn
hooks:
  - on-evaluate
check: |
  Apply the project's preferred review strictness to code review and milestone evaluation.
  Trigger when tests pass but review-quality concerns remain unaddressed.
  Recommend resolving correctness, regression, and maintainability findings before continuing.
