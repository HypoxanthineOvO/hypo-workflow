name: auto-continue-threshold
label: workflow
description: Govern automatic continuation decisions after evaluation.
default_severity: warn
hooks:
  - on-evaluate
check: |
  Compare evaluation scores with `evaluation.max_diff_score` and auto-continue settings.
  Trigger when an automatic continue would bypass a warning-level or higher score.
  Warn by default; error severity makes the evaluation checkpoint a hard gate.
