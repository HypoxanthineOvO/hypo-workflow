name: git-clean-check
label: guard
description: Check whether the Git working tree has uncommitted changes before milestone execution.
default_severity: warn
hooks:
  - pre-milestone
check: |
  Run `git status --porcelain`.
  If the output is not empty, trigger this rule.
  Suggest committing, stashing, or explicitly acknowledging unrelated changes before continuing.
