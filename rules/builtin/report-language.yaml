name: report-language
label: style
description: Keep reports and generated summaries aligned with output.language.
default_severity: warn
hooks:
  - always
check: |
  Read effective `output.language` from project config, then global config, then defaults.
  User-facing reports, PROGRESS entries, status text, and interaction prompts should use that language.
  Internal state keys in YAML remain English.
