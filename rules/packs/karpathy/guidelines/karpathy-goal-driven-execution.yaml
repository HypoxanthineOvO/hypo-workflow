name: karpathy-goal-driven-execution
label: guideline
description: Define validation up front and execute toward explicit user-visible goals.
default_severity: off
pack_default_severity: warn
hooks:
  - always
check: |
  Goal-Driven Execution: define the desired effect and verification method before
  implementation, then evaluate progress against that acceptance target.
