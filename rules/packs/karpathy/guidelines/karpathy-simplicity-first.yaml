name: karpathy-simplicity-first
label: guideline
description: Prefer the simplest approach that satisfies the user requirement.
default_severity: off
pack_default_severity: warn
hooks:
  - always
check: |
  Simplicity First: avoid unnecessary abstraction, ask whether a smaller solution
  would satisfy the user, and keep the implementation proportional to the scope.
