name: karpathy-surgical-changes
label: guideline
description: Keep code changes tight, local, and compatible with surrounding patterns.
default_severity: off
pack_default_severity: warn
hooks:
  - always
check: |
  Surgical Changes: modify the smallest reasonable surface area, preserve local
  conventions, and avoid opportunistic refactors unless they are required.
