---
description: Configure Hypo-Workflow global defaults in ~/.hypo-workflow/config.yaml
---

Load `skills/setup/SKILL.md` from this plugin and run the `/hypo-workflow:setup` flow.

This command configures Hypo-Workflow itself, not a project-local `.pipeline/` workspace. Use it to create or update `~/.hypo-workflow/config.yaml`, then summarize the effective global defaults.
