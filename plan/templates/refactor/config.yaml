pipeline:
  name: "Refactor Template"
  source: local
  output: local
  prompts_dir: prompts
  reports_dir: .pipeline/reports

execution:
  mode: self
  steps:
    preset: custom
    sequence:
      - implement
      - review_code

evaluation:
  auto_continue: false
  max_diff_score: 3
