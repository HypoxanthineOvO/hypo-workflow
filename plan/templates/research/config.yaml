pipeline:
  name: "Research Template"
  source: local
  output: local
  prompts_dir: prompts
  reports_dir: .pipeline/reports

execution:
  mode: self
  steps:
    preset: implement-only

evaluation:
  auto_continue: true
  max_diff_score: 3
