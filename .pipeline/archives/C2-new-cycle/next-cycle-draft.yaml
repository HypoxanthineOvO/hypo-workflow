version: 1
status: draft
created_at: "2026-05-02T00:29:20+08:00"
source: "/hw:chat"
target_cycle:
  id: C3
  name: "OpenCode Multi-Agent Model Matrix"
  type: feature
  first_task:
    id: F001
    title: "OpenCode Multi-Agent Model Matrix"
    priority: 10
    gate: confirm
    decompose_mode: upfront
    summary: "Make OpenCode agent/model routing a first-class Hypo-Workflow configuration surface instead of hand-editing opencode.json."
    desired_effect:
      - "Plan work can use GPT-5.5 through the OpenCode plan agent."
      - "Context compaction can use a cheaper Flash-class model such as DeepSeek V4 Flash or Mimo V2.5 Flash."
      - "TDD work can route testing, two code workers, debug/review, and report writing to distinct OpenCode subagents."
      - "OpenCode compaction should begin around a 900K-token effective context target."
    implementation_notes:
      - "Extend the config schema with an OpenCode agent model matrix and compaction settings."
      - "Generate additional OpenCode agents such as hw-test, hw-code-a, hw-code-b, and hw-report."
      - "Render agent-level model fields and agent.compaction into opencode.json through hypo-workflow sync."
      - "Keep Hypo-Workflow as a setup/sync layer, not a model-calling runner."
    acceptance:
      - "hypo-workflow sync --platform opencode emits the requested model matrix deterministically."
      - "Generated opencode.json validates with opencode debug config when OpenCode is installed."
      - "Docs explain which routing is enforced by OpenCode config and which remains prompt discipline."
      - "Existing OpenCode command, agent, plugin, and regression tests still pass."
