# 步骤：implement

## 目标

阅读当前 Prompt 的 `需求` 部分，实施满足测试的最小正确改动集。

## 指令

1. 阅读当前 Prompt 的 `需求` 和 `预期产出`。
2. 实现代码，使目标测试通过。
3. 遵循宿主项目既有架构、命名和风格。
4. 除非能明确指出测试错误，否则不要修改测试。
5. 优先选择满足 Prompt 的最简单实现。

## 输出备注

记录到 state 和 log：

- 新增文件
- 修改文件
- 主要实现决策
- 剩余风险或后续工作
