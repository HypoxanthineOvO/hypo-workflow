# 步骤：write_tests

## 目标

阅读当前 Prompt 的 `预期测试` 部分，编写本轮所需测试。

## 指令

1. 阅读当前 Prompt 文件，重点关注 `预期测试`。
2. 推断宿主项目需要的最小测试文件集合。
3. 覆盖：
   - 正常路径
   - 边界情况
   - 错误处理
4. 创建或扩展测试文件。
5. 本步骤不要实现生产代码。
6. 如果 Prompt 没有提供足够测试意图，停止并说明缺失信息。

## 输出备注

记录到 state 和 log：

- 创建或修改的测试文件
- 约新增测试用例数量
- 有意延后的未覆盖风险
