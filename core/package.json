{
  "name": "@hypo-workflow/core",
  "version": "10.0.2",
  "private": true,
  "type": "module",
  "description": "Shared deterministic helpers for Hypo-Workflow platform adapters. This package is not a runner.",
  "bin": {
    "hw-core": "./bin/hw-core"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
