question: "How should analysis milestones persist recoverable state and reviewable evidence?"
environment_snapshot:
  branch_commit: C3-fixture-commit
  effective_config_summary: "execution.steps.preset=tdd; workflow_kind=analysis"
  command_parameters:
    command: /hw:resume
    prompt_file: .pipeline/prompts/05-analysis-state-ledger-format.md
  data_log_sources:
    - references/state-contract.md
    - references/analysis-spec.md
    - .pipeline/state.yaml
  time_window: "2026-05-02T12:56:40+08:00..2026-05-02T13:00:00+08:00"
  model_provider_parameters:
    platform: opencode
    subagent_provider: codex
hypotheses:
  - id: H1
    statement: "state.yaml should hold only the active execution pointer plus a compact analysis summary."
    status: confirmed
    evidence_refs:
      - O1
  - id: H2
    statement: "state.yaml should embed all hypotheses and observations for complete resume context."
    status: disproved
    evidence_refs:
      - O2
experiments:
  - id: E1
    hypothesis_refs:
      - H1
    method: "Compare state, feature queue, log, and metrics contracts."
    status: completed
    command: "node --test core/test/analysis-state-ledger.test.js"
    observation_refs:
      - O1
  - id: E2
    hypothesis_refs:
      - H2
    method: "Check whether full evidence arrays are required for watchdog/status/resume."
    status: completed
    command: "rg \"state.yaml\" references"
    observation_refs:
      - O2
observations:
  - id: O1
    summary: "Resume needs prompt, step, heartbeat, and a pointer to durable evidence."
    supports:
      - H1
  - id: O2
    summary: "Full hypotheses and experiments belong in an external review ledger."
    contradicts:
      - H2
metrics:
  duration_ms: n/a
  token_count: n/a
  cost: n/a
interpretation: "A lightweight state summary keeps automation recoverable while the ledger keeps analysis auditable."
conclusion: "Keep state linear and store full analysis evidence in a dedicated ledger."
confidence: high
next_actions:
  - "Use the ledger path from state summaries in status, report, and watchdog surfaces."
code_change_refs:
  - references/analysis-ledger-spec.md
threats_to_validity:
  - "The fixture proves shape and parser support, not every downstream renderer."
ruled_out_alternatives:
  - "Embedding complete evidence chains in .pipeline/state.yaml."
