# s53 — Global CLI/TUI Setup

Validate the setup-only `hypo-workflow` command.

- First setup writes global config.
- Profile list/use/edit works in non-interactive mode.
- Doctor reports platform availability.
- Sync/init-project can generate OpenCode artifacts through core.
- CLI help states it is not a runner.
