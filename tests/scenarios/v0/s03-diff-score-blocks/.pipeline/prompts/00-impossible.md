# 00-impossible

## 需求

实现一个 `choose()` 函数：

- 同一次调用必须返回字符串 `"ok"`
- 同一次调用也必须返回字典 `{"status": "ok"}`
- 不允许增加参数，也不允许修改调用方式

## 预期测试

- `choose()` 等于字符串 `"ok"`
- `choose()` 等于字典 `{"status": "ok"}`
- 实现应保持单一返回值语义

## 预期产出

- 新增 `chooser.py`
- 提供 `choose()` 函数
- 如果需求无法兼容，review 需要明确指出架构冲突
