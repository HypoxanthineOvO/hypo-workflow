pipeline:
  name: "System Test S06"
  source: local
  output: local

execution:
  mode: self
  steps:
    preset: custom
    sequence:
      - implement
      - review_code

evaluation:
  auto_continue: false
  max_diff_score: 3
  checks:
    - matches_plan
    - code_quality
